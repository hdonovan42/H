# 2026-05-11

## Portfolio
- move README out of H (lives in PORTFOLIO repo now) (`37af708`)
- expand README with command reference and new-machine bootstrap (`4e76e81`)
- introduce PORTFOLIO.py CLI with vault-only data redundancy (`cb6d19d`)
- BUY NVDA 1 (`1c1a490`)
- BUY MSFT 3 (`09dc27c`)
- BUY AAPL 5 (`6874453`)
- SELL TSLA 4 (`3c38133`)
- BUY TSLA 10 (`aefb653`)

## VAULT
- SELL exception-path must verify CTF burn, not USDC delta alone (`cf9fbdd`)
- loosen v2 share-verify thresholds + auto-correct partial fills (`23843da`)
- redemption_sweep must skip sold/cancelled positions (`c916617`)
- drawdown circuit-breaker 25% -> 50% (`d01c371`)
- SELL stealth-fill detection (mirrors v20.1 BUY logic) (`ab69c3a`)
- disambiguate v_1h in momentum logs (YES_v_1h prefix) (`a521960`)
- log v20.8.1 follow-up in CHANGELOG (`2d8bfd6`)
- settlement-adapter pUSD allowances (`c8f9e9e`)
- deploy: register contract-check timer + comment on dual-service restart (`f84542f`)
- comprehensive Polymarket v2 migration audit (`4b8a764`)
- real-money execution-layer hardening (`fb6f773`)
- CLOB protocol v2 support (`10617a1`)
- stop silent 50/50 fallback in gamma API parser (`a399d76`)
- dashboard: surface daemon liveness on reconciliation panel (`0e3ef2a`)
- wallet sync must classify redemption sources as internal (`196d396`)
- backfill missing shadow_trades schema (`af4c630`)
- on-chain redemption + auto-redeem on resolve (`4bcb542`)
- stealth-fill detection via on-chain CTF poll (`81a8008`)
- FOK -> FAK orders, no cooldown on dry-run, drop startup dry-run cycles (`3c437a1`)
- dashboard: fix status-bar layout for 6 boxes (was wrapping ugly at mid widths) (`5fedc20`)
- dashboard: drop ARR, add Cash, reorder top stats (+ TIME ALIVE) (`1c365ff`)
- cashflow: drop Tx column from UI (hash still stored in DB for audit) (`79d4412`)
- cashflow panel: label Direction column 'deposit'/'withdrawal' (was IN/OUT) (`797b8ae`)
- fix sub-$1 momentum bets — bump base sizing + client-side CLOB min guard (`825fbbf`)
- label CCTP mint counterparty (0x0) as 'Bridge mint (CCTP)' in cashflow (`a7ef413`)
- dashboard: move Reconciliation panel to bottom of Home, drop left accent border (`ffdbc6a`)
- reset disables foreign_keys during wipe to bypass FK blocks (`9bbb803`)
- wallet-level cashflow tracking + accounting reset (`1fbc933`)
- auto-swap native USDC -> USDC.e on deposit (`45ca0a4`)
- check_allowances reads on-chain directly (CLOB API was lying) (`1d66e96`)
- vault go-live no longer requires simulated:false (`ae02011`)
- disable on-chain % cap; use $10 deposit as real safety measure (`95e2e9f`)
- real-money safeguards after 15 Mar 2026 $50 loss (`2348c20`)
