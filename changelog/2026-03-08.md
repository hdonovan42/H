# 2026-03-08

## Chess
- shrink text 20% when viewport scale >= 1x (`97b0ecb`)
- cap viewport scale at 1.2x (`e3bf115`)
- cap viewport scale at 1.5x (`83773d7`)
- increase viewport scale cap from 1x to 2x (`f88ccba`)
- migrate eval graph from Canvas 2D to Chart.js 4.x (`ebb1735`)
- add Syzygy endgame tablebase integration (`283fcaa`)
- engine optimisations — depth 18, hash tables, batched UI (`bdcbe6a`)
- performance & cleanup pass — 8 optimisations (`7b22a30`)
- increase analysis depth 15→22 for accurate accuracy scores (`a46a32a`)
- delete V3 dead code — remove 2,295 lines of unshipped modules (`a651946`)
- remove debug logging, auto-flip working (`46e27f9`)
- add orientation tracking debug (`a990c05`)
- remove double-flip — config orientation already works (`c5d9e78`)
- add debug logging for flip (`7bb70e1`)
- call flipBoard() after Lichess load when color=black (`d599ee3`)
- use color param for board orientation instead of post-load flip (`6cddbd9`)
- major performance overhaul — persistent worker, FEN cache, split DOM updates (`2447383`)
- fix extension auto-flip — use class selector for orientation detection (`95be28a`)
- fix auto-flip detection using Lichess board orientation (`4db434d`)
- rename extension button to 'hjd' (`d19bb75`)
- auto-flip board when user played black (`6dcad8b`)
