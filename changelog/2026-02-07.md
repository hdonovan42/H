# 2026-02-07

## Add
- pipeline kill button and fix proposal re-selection bug (`2a6f002`)
- AXIOM v2: self-recursive capability system with first verified capability (sp-backup) (`33e37de`)
- AXIOM agent tools (code exec, file I/O, HTTP, shell) and capability-building prompt shift (`a2bf090`)

## AXIOM
- implementer cost reduction: history trimming, message caching, round cap (`316df9b`)
- token optimisations: prompt caching, selector single-call, output truncation (`e2d89ef`)

## Migrate
- cross-value compounding from actuator-bridge to values.json (`bb6329d`)

## Remove
- Bearer token auth from shell routes — nginx basic auth suffices (`2ea8f39`)
