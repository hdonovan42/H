# Changelog

One file per day a commit was made (`YYYY-MM-DD.md`), commits grouped by project.
Succinct by design — each bullet is the commit subject + short hash.

Regenerate from git history at any time:

```bash
python3 changelog/_generate.py   # run from repo root
```
