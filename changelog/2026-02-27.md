# 2026-02-27

## Add
- Clear button and fix Cancel to clear localStorage draft (`ec4eb8a`)
- admin alerting, SSS schema validation & APK version watcher (`dfc73d9`)
- CSS Core Search fallback scraper + gitignore decompiled APK (`b8f62dc`)
- confirm dialog before deleting a parked search (`198bfd7`)
- repo-wide CLAUDE.md with workflow orchestration rules (`1397113`)

## All-In
- subscription copy — "1 additional ACTIVE search slot, cancel any time" (`968729f`)

## Bind
- Express to 127.0.0.1 — all traffic must go through nginx (`f1ced21`)

## Combined
- body type + doors dropdown — filter by door count (e.g. 3dr/5dr Hatchback) without extra form field (`533a79c`)

## Dynamic
- faceted dropdowns for body type, fuel, transmission, colour (`cfb89ca`)

## Fix
- Settings phone number showing as unsaved on page load (`34b25f3`)
- search card action buttons text alignment — zero horizontal padding for centred labels (`b521eb0`)
- search editor grid alignment — prevent long select options from stretching columns (`84030d8`)
- facet dropdown labels — use value/displayName from CWS response (`66535d4`)
- search card actions stretching when listings panel expands (`a59d410`)
- write-off category exclusion — correct CWS API parameter name (`4e701e3`)
- search card actions layout — symmetrical spacing, matching button sizes, shorter divider (`1a13276`)

## Immediate
- poll on search create/edit + stale listing cleanup (`5b83381`)

## Paginate
- CWS API results + use search criteria for fuel/transmission (`d627ba8`)

## Parked
- search cards — replace status label with Unpark button, add red Delete button (`69b2d07`)

## Persist
- search form drafts to localStorage across refreshes (`8b55523`)

## Phase
- 0 critical fixes — race conditions, shell injection, webhook idempotency (`8cec2bf`)

## Reorder
- body type dropdown — default order with count sort on criteria (`e93cbde`)

## Revert
- admin token middleware — nginx basic auth is sufficient (`5912e8a`)

## Route
- dashboard to #/ and rename nav link to Home (`63b4408`)

## Search
- editor dropdown improvements — merge static+facet options with counts, sort by popularity, alphabetical variants (`c550631`)

## Skip
- notifications on criteria edit, hard-delete searches on DELETE (`8df785e`)

## Switch
- from hash routing to History API for clean URLs (`00e5572`)

## Tone
- down saved button — transparent bg with dim text instead of bright green (`3974efe`)
