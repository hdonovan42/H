# 2026-02-26

## Add
- post-deploy health check and fix stale file sync (`a721232`)
- CAT vehicle exclusion toggle and rearrange editor layout (`6208525`)
- analysis scripts, skill files, and AutoSnipe v1.3.0 changelog (`f71678c`)
- live Autotrader taxonomy system — 42 makes, 1133 models, 7924 trims (`3df6c74`)
- model variant support to AutoSnipe search filters (`65c5da9`)
- Cloudflare solver and computer use cost optimisations (`a456be2`)
- number formatting and make postcode/distance optional (`8fb0769`)
- AutoSnipe link to projects page (`efcc2af`)
- sign-out confirmation and model dropdown to search editor (`4219f43`)

## Align
- dashboard section headers with min-height (`eea59a3`)

## All-In
- landing page copy for AI Monitors feature (`4c9674b`)

## CAT
- toggle: neutral default, subtle red glow when included (`5417463`)

## Dynamic
- placeholder for search name field (`fceb2ca`)

## Email
- notifications + phone number validation + saved state feedback (`8122b6d`)

## Fix
- scheduler datetime SQL bug and switch to 5-minute poll interval (`e6f21b7`)
- auth session persistence and HTML-as-JSON parse errors (`a1f2d85`)
- AutoSnipe deploy Node version mismatch (`91b1077`)
- deploy Node version mismatch and populate VW models (`512f041`)
- package-lock.json sync for puppeteer dependencies (`8af1c05`)

## Full-width
- dashboard with collapsible inline listings (`6199350`)

## Hide
- scrollbars globally for cleaner dark UI (`da27b86`)

## Keep
- old result count visible while new count loads — no flash on update (`e95c28b`)

## Lighten
- UI colours and redesign search card layout (`17bc0b2`)

## Live
- result count in search editor title — debounced CWS API count as criteria change (`e8ce383`)

## Make
- VAULT dashboard fully public, remove all auth (`bae856b`)
- VAULT landing page public, keep API behind basic auth (`a2cffd6`)

## Monthly
- subscription paywall + currency detection fix + UI redesign (`bb15d3b`)

## Remove
- listing image thumbnails from match feed for cleaner text-only display (`24ab9f8`)
- tier badge and slots counter from navbar, add active page underline (`222dce8`)

## Replace
- HTML scraping with GraphQL API + Cloudflare session (`e374ada`)
- subscription tiers with pay-per-slot model (v1.1.0) (`46a784e`)

## Revert
- UI redesign — restore original dashboard, search cards, and CSS (`107941e`)

## SearchEditor
- 3x4 grid + SearchCard nickname-aware tags (`8d0a760`)

## Shorten
- disabled dropdown placeholders (`9d621bd`)

## Show
- variant dropdown by default, disabled until model with variants selected (`e9deb8d`)

## Simplify
- payments to GBP-only — remove geo-currency detection and USD/EUR support (`1f74a6d`)

## Subtle
- CAT toggle styling — no green, use muted border/text (`4b6b92f`)
