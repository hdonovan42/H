# 2026-02-28

## Add
- terminal-style border and glow to viewing bar (`296a097`)
- PreviewFrame terminal chrome to listing popup window (`f43efc2`)
- changelog — momentum churn fix + 2dp formatting (`21d6fee`)

## Apply
- terminal-style border to listings panel (`2fc3316`)

## Fix
- nav popup positioning — force top-right with moveTo/resizeTo (`a9d2f59`)
- 18s dashboard load — remove dead smart-money fetch from useVaultData (`6b0c9b4`)

## Inline
- viewing bar — terminal nav inside search card listings panel (`272b216`)

## Keep
- dashboard focused after opening popup so viewing bar is visible (`9f74906`)

## Move
- listing nav to fixed overlay in main window (`9de84cb`)

## Nav
- bar as separate popup window above Autotrader (`1222d79`)

## Open
- listings in positioned popup window instead of new tab (`937a244`)

## Remove
- listing image thumbnails — popup handles visuals (`37d747f`)

## Rename
- Dashboard nav link to Home (`abc0392`)

## Replace
- iframe with listing summary card in popup (`f17b45b`)

## Revert
- to direct window.open — Cloudflare blocks server-side proxy (`34f3a30`)

## Server-side
- frame proxy — PreviewFrame wraps proxied Autotrader content (`343b00a`)

## Skip
- intermediary page — redirect straight to Autotrader on popup open (`5f009cf`)

## Standardise
- cost display to 2dp and skip momentum positions in exit gate (`9a9356e`)
