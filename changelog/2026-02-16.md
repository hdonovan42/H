# 2026-02-16

## VAULT
- z-score as confidence modifier + outcome tracking columns (`ed72e88`)
- z-score as confidence modifier, not sizing multiplier (`65ed5f2`)
- z-score as sizing amplifier, not entry gate (`f64c18a`)
- CHANGELOG: z-score normalized velocity (`bad91b3`)
- z-score normalized velocity — suppress sports noise, amplify real signals (`f4ed60d`)
- position-aware momentum — add/hold, not repeat entry (`11321e3`)
- fix momentum signal quality — sort by |v_1h|, live odds, less cautious Haiku (`b227131`)
- pure velocity-following momentum system (`30c0938`)
- pre-filter 99.5%+ markets from momentum pipeline (`78dd633`)
- fold intel bets into legacy, remove INTEL card from dashboard (`91f768b`)
- disable tweets, sentinel & intelligence — momentum-only pipeline (`c58d6d0`)
- collapse closed positions by default on dashboard (`63f4a45`)
- show INTEL PAUSED indicator on dashboard (`95bc568`)
- fix prediction source tagging — check entry_edge not just entry_confidence (`0121839`)
- disable intel bets, momentum only — save ~$0.20/day Opus cost (`7b43e70`)
- hard 99.5% price ceiling + decider entry gate for opportunity cost (`fa81231`)
- double momentum bet sizing — max $4, 10% balance fraction (`d1c6d8e`)
- tiered downsampling for balance lifeline API (`4847c82`)
- reconstruct historical positions value in balance lifeline (`7dba836`)
- fix balance lifeline — use cycle snapshots instead of retroactive replay (`9e8fbdc`)
- exit open positions that no longer meet current entry minimums (`37206f8`)
- use actual market end_date for risk-free comparison instead of assuming 30 days (`348fea1`)
- tighten intel entry — min_confidence 0.30 → 0.40 (`1615fab`)
- show ROI % next to net gain in source summary cards (`07a20d0`)
- fix W/L counts — sold positions classified by P&L not as losses (`349bb9a`)
- Universal market scanning + opportunity cost exits (`1040a37`)
- add pyramiding — scale bet size with unrealised ROI (`e05a1d0`)
- Momentum-First Architecture changelog (`1e6c7e3`)
- raise momentum exposure cap to 50% of balance per market (`66d4edc`)
- remove momentum price ceiling — capital efficiency exit is sufficient (`c1bed8a`)
- replace momentum dedup with smart guardrails — scale in, don't stack blindly (`0da1bfc`)
- collapse legacy positions — minimised by default, click to expand (`e2900e0`)
- show odds to 2 decimal places in positions panel (`6fa3db6`)
- raise capital efficiency exit threshold to 99% (`2974b7e`)
- capital efficiency exit — sell positions at >= 95% max value (`3c51eba`)
- source tags on positions — INTEL vs MOMENTUM vs LEGACY (`cb69d80`)
- only fire vetoes when there's an actual bet to block (`726bfee`)
- mark-to-market smart money stats for pending entries (`e409a68`)
- fix momentum bet stacking — skip markets with open positions (`8cbe870`)
- Performance Mirror — feed track record into Opus intelligence (`f848176`)
- Smart Money Velocity system (`70f4552`)
- add snapshot fix to changelog (`295614d`)
- record odds snapshots for positions/estimates missed by discovery (`722813f`)
