# 2026-02-15

## VAULT
- changelog entry for tonight's session (`9b40412`)
- fetch odds for estimated markets missed by discovery (`0fb5d9d`)
- lower margin_of_safety 0.15→0.10, sync min_confidence 0.3 (`d92759e`)
- lower min_confidence 0.4→0.3, track entry_confidence (`7e20469`)
- filter auto-hold server-side instead of frontend (`b2e1319`)
- filter auto-hold cycles from dashboard Recent Decisions (`ca14d5d`)
- fix nothing-to-commit detection (stdout not stderr) (`9cf6442`)
- git-backed intelligence history, prune DB to single row (`5380e3d`)
- changelog entry for intelligence document fixes (`40a6968`)
- cap master doc at 1500-2000 words with decay/prune rules (`8fd8716`)
- increase Opus max_tokens 4096→8192 for longer master doc (`2db8c4c`)
- fix Opus intelligence prompt to edit existing doc, not rewrite (`891d239`)
- remove emergency update cooldown — the tighter prompt is the fix (`e8fcced`)
- tighten sentinel major_event + 6h cooldown + date fix (`80312ea`)
- odds velocity tracking — detect sharp market moves (`afd61a1`)
- reduce cycle interval from 10min to 1min (`ea9cff8`)
- change burn rate to rolling 24h window (`18a5a55`)
- auto-pilot decider — skip Claude when nothing to decide (`81a81ad`)
- exclude WAL/SHM files from rsync --delete in deploy (`f4c4cf8`)
- fix balance chart to mark predictions to market with live odds (`3e442fe`)
- stable Opus estimates + Haiku sentinel (`42a0fa8`)
- add v7 changelog entry for Opus master intelligence pipeline (`496d791`)
