# 2026-03-09

## AutoSnipe
- auto-prune sold/expired listings during poll cycle (`eb5fc99`)

## Chess
- match settings btn and toggle height to engine select (26px) (`733e803`)
- snap zoom to nearest 5% before incrementing (`aab2c84`)
- fix mate score clipping on eval graph (`cfd7f5e`)
- height-aware auto-scaling + manual zoom control (`47b2642`)
- show mate notation in graph tooltip instead of +10.0 (`8f04724`)
- cap viewport scale at 1x, remove text shrink (`094c1c1`)
- add breathing room to eval graph edges (`f651df9`)
- add dotted zero line to eval graph (`cf1a100`)
- hide inaccuracy/mistake dots from eval graph (`55d634f`)
- make pgn-hint font-size relative so it respects scale shrink (`89fc627`)
