# 2026-06-05

## changelog
- per-day changelog folder (one file per commit-day, grouped by project) (`68e29ac`)

## WaymoWatch
- daily digest + tightened surfacer (cut FP firehose) (`887c4a5`)
- migrate live watch to VPS + 09:00-21:00 London window (`1d77b63`)
- widen live watch to Park Royal depot (KX 70 + PR 25 cams) (`00786da`)
- digest batch 20 -> 5 (hear about possible Waymos within ~1-2h) (`5325f4a`)
- widen live watch to ~70 KX cameras + email every 20 high-prob (`8b7f2d8`)
- high-probability-only surfacing (stop collecting non-Waymos) (`ad12827`)
- --collect mode (wipe + gather N distinct white cars, email) (`6c7a30d`)
- ByteTrack within-clip dedup -> one row per vehicle (`d9ec389`)
- one entry per vehicle (dedup-with-update) (`bbbddeb`)
- fix digest count/sheet mismatch (`bd69817`)
- Stage-2 negative harvester (stratified white-non-Waymo) (`4e31aae`)
- two-stage cascade — white-car Stage-1 filter + whole-car crops (`65f485a`)
- email alerts (Resend) for King's Cross candidates (`68a455c`)
- local King's Cross capture (cron) + you-only WhatsApp wiring (`4d3ca92`)
- scope live-capture to King's Cross / British Library (manual phase) (`ff90488`)
- live-capture loop (real on-distribution positives) (`f231d96`)
- inferencePlan.md + dataset quality trim (host >=44px) (`ef27d61`)
- high-quality dataset + GPU training recipe (`17f6c72`)
