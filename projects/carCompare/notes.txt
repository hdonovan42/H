- use llm to build data.js. 
how is this best done

different prompt for each automaker?
which api is best for this task?

-create validation for each car brand. only push 100% score model/prompt.

-structured generation (json enforcement) -> RAG (web) -> verification pipeline (multi-source cross checking)

